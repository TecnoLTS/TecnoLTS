services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tecnolts-app
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      edge:
        aliases:
          - tecnolts-app

networks:
  edge:
    external: true
    name: edge
